defmodule AuthenticationHelpers do

  # def authenticate_user(conn), do: verify(conn.session[:user_id])

  # defp verify(user_id) when user_id == nil, do: halt! conn.status(401)
  # defp verify(user_id) do
  #   User.find(user_id)
  #   conn.status(200)
  # end
end
